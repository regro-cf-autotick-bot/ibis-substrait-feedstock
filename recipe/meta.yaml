{% set name = "ibis-substrait" %}
{% set version = "2.24.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ibis_substrait-{{ version }}.tar.gz
  sha256: 092ad3f50f94d873735c91c046e2bc014ba3fc7a6042177d93bbec80a0f4fd2e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - python >=3.8
    - pip
    - poetry-core
  host:
    - pip
    - python >=3.8
    - poetry-core
  run:
    - ibis-framework-core >=3
    - protobuf ==3.20.1
    - python >=3.8
    - sqlalchemy >=1.4,<2

test:
  imports:
    - ibis_substrait

about:
  home: https://github.com/ibis-project/ibis-substrait
  summary: Subtrait compiler for ibis
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - gforsyth
    - cpcloud
